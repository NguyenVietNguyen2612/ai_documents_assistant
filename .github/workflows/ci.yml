name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # ==========================================
  # PHẦN CI: KIỂM TRA (INTEGRATION)
  # ==========================================
  test-backend:
    name: Run Backend Tests (CI)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12' # Đã cập nhật lên 3.12 để hỗ trợ numpy mới nhất
          cache: 'pip' # Bật tính năng cache thư viện
          cache-dependency-path: 'backend/requirements.txt' # Trỏ tới file requirements để Github theo dõi sự thay đổi

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install pytest

      - name: Start Database Services
        run: |
          # Khởi động Milvus (standalone) và các dependency của nó (etcd, minio) ở chế độ nền
          docker compose up -d standalone
          # Đợi 15 giây để Milvus khởi động hoàn toàn trước khi test
          sleep 15

      - name: Run Pytest
        env:
          GEMINI_API_KEY: "dummy_key_for_testing_purposes"
        run: |
          cd backend
          python -m pytest auto_tests/

  # ==========================================
  # PHẦN CD: ĐÓNG GÓI VÀ PHÂN PHỐI (DELIVERY)
  # ==========================================
  build-and-push:
    name: Build & Push Images (CD)
    needs: test-backend # BẮT BUỘC job CI phải pass thì job CD mới được chạy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Backend Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/ai-documents-backend:latest

      - name: Build and Push Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/ai-documents-frontend:latest
